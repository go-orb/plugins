org:
  orb:
    svc:
      client:
        logger:
          plugin: slog
          format: json
          level: INFO # DEBUG-1
        client:
          logger:
            level: INFO # DEBUG-1
      service:
        logger:
          plugin: slog
          format: json
          level: INFO # DEBUG-1
