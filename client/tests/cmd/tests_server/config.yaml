org:
  orb:
    svc:
      client:
        logger:
          plugin: slog
          format: json
          level: WARN
        client:
          logger:
            level: WARN
      service:
        logger:
          plugin: slog
          format: json
          level: WARN
