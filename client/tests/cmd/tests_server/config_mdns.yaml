org:
  orb:
    svc:
      client:
        logger:
          plugin: slog
          format: json
          level: TRACE # DEBUG-1
      service:
        logger:
          plugin: slog
          format: json
          level: TRACE # DEBUG-1
